index.html for the webpage, contains the input elements
script.js for the logic of the inputs, listeners for inputs & data exchange
server.js for receiving the data & writing it to the sqlite3 db
todos.db for storing the data 

Problems: 
Can delete them from the webpage
Only see them after you press "add tasks", blank on reload till u add another

Next up:
-Make it show the data you've already added, add task titles & extra details, potentially add options for links/images/text files to be added alongside the files, then to allow the data to be sorted into different tables such as "ideas" or "projects" etc.
-Plan future project where this can be integrated with project "noodle"
-Style up the page